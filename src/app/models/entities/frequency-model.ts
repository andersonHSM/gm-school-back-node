export interface FrequencyModel {
  frequency_guid: string | ArrayLike<number>;
  class_has_discipline_has_schedule_guid: string | ArrayLike<number>;
  user_guid: string | ArrayLike<number>;
  isPresent: boolean;
}
